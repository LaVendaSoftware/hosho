en:
  true: "Yes"
  false: "No"
  nil: "Empty"
  form:
    submit:
      create: "Create %{model}"
      update: "Update %{model}"
      save: "Save %{model}"
  alerts:
    create:
      success: "Record created successfully."
    update:
      success: "Record updated successfully."
    destroy:
      success: "Record destroyed successfully."
  activerecord:
    attributes:
      created_at: "Created at"
      updated_at: "Updated at"
  time:
    formats:
      custom: "%b %d, %Y, %I:%M:%S %p"
    distances:
      past: "%{time} ago"
      future: "In %{time}"
