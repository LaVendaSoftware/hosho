<%= render(Components::PageTitle::Show.new(record: @category, back_url: categories_path)) %>

<div class="flex flex-col gap-4 p-4 md:gap-6 md:p-6">
  <%= render("category", category: @category) %>
</div>
