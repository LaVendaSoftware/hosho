<%= render(Components::PageTitle::Index.new(model: Order, link: new_order_path, icon: nil)) %>

<% if @orders.blank? %>
  <%= render(Components::Alerts::NotFound::Male.new(model: Order)) %>
<% else %>
  <%= render(Components::Table::Root.new(
        records: @orders,
        heads: [
          ta(Order, :status),
          ta(Order, :customer_name),
          ta(Order, :customer_email_address)
        ],
        columns: [
          :humanized_status,
          :customer_name,
          :customer_email_address
        ],
        actions: [:link_to_edit, :link_to_show, :link_to_destroy]
      )) %>

  <%= render(Components::Pagination::Links.new(records: @orders)) %>
<% end %>
