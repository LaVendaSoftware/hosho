<div id="<%= dom_id(customer) %>">
  <%= render(Components::Page::Show.new(
        record: customer,
        columns: [
          :user_name,
          :user_email_address,
          :country_code,
          :phone,
          :document
        ]
      )) %>
</div>
